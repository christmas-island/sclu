services:
  sclu-bot:
    build: .
    env_file: .env
    restart: unless-stopped

  sclu-api:
    build: .
    command: python api.py
    env_file: .env
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      - API_PORT=8080
